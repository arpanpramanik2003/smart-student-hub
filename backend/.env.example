# ===========================================
# SMART STUDENT HUB - BACKEND CONFIGURATION
# ===========================================
# Copy this file to .env and fill in your values

# ===========================================
# SERVER CONFIGURATION
# ===========================================
PORT=5000
NODE_ENV=development

# ===========================================
# SECURITY CONFIGURATION
# ===========================================
# For production, generate a new secret with:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=24h

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# Local development: SQLite (default)
DB_NAME=smart_student_hub.db

# Production: PostgreSQL (Supabase)
# Uncomment and add your Supabase connection string:
# DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.xxxxx.supabase.co:5432/postgres

# ===========================================
# CORS CONFIGURATION
# ===========================================
# Allowed frontend origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# ===========================================
# GOOGLE DRIVE STORAGE (Production Only - 2TB!)
# ===========================================
# Leave commented for local development (uses local file storage)
# Uncomment for production deployment:

# Your Google Drive folder ID (from folder URL)
# GOOGLE_DRIVE_FOLDER_ID=1aBcDeFgHiJkLmNoPqRsTuVwXyZ

# Service account JSON credentials (paste entire JSON)
# GOOGLE_DRIVE_CREDENTIALS={"type":"service_account","project_id":"..."}

# ===========================================
# EMAIL CONFIGURATION (Optional)
# ===========================================
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-app-password
# EMAIL_FROM=noreply@smartstudenthub.com

# ===========================================
# DEVELOPMENT NOTES
# ===========================================
# 1. SQLite is used by default for local development
# 2. PostgreSQL (Supabase) is used when DATABASE_URL is set
# 3. Local file storage is used by default
# 4. Google Cloud Storage is used when GCS_CREDENTIALS is set
# 5. Run 'npm run create-admin' to create an admin account
# 6. See SETUP_CREDENTIALS.md for getting production credentials
